# 最小構成リポジトリ分離計画: DEVA方式 SAM2+BEN2/TB ハイブリッド動画アプリ

- 調査日: 2026-07-01（2026-07-01 夜に対象アプリ変更を反映して更新）
- 対象: `gradio_app_sam2_ben2_tb_hybrid_deva_manual_for_Movie.py`（手動プロンプト版）
  のみを親会社へ渡す前提の最小リポジトリ構成調査
  - ※当初調査時点の対象は `gradio_app_sam2_ben2_tb_hybrid_deva_for_Movie.py`（自動DEVA版）
    だったが、ユーザー指示により渡すアプリを手動プロンプト版へ変更。パイプライン本体
    （`route_a_deva_hybrid_video_pipeline.py` / ハイブリッドalpha生成）は両者で共通のため、
    依存チェーンの大部分は変わらない。
- 前提: 本ファイルは調査記録であり、実際の新規リポジトリ作成・ファイルコピーはまだ実施していない（要別途承認）
- 決定事項（ユーザー指示）:
  - checkpoints（重み本体）は配布リポジトリから**除外**する（README にDL手順のみ記載）
  - tests/ は配布リポジトリに**含めない**
  - ライセンス表記は本ファイルではなく
    `調査\2026-07-01_使用モデルチェックポイントサイズとVRAM概算.md` に追記する

## 1. 対象アプリの依存チェーン（コード確認済み）

```
gradio_app_sam2_ben2_tb_hybrid_deva_manual_for_Movie.py
  ├─ pipelines/components/model_components.py（GroundingDINOMultiBoxDetector: 手動候補box検出用）
  ├─ pipelines/components/route_a_common.py（config/route_a.toml 読込）
  ├─ pipelines/components/ui_helpers.py（手動 box/point プロンプト Canvas 用UIヘルパー、純関数群）
  │     └─ pipelines/components/common.py（ensure_rgb_array）
  ├─ pipelines/components/video_common.py（normalize_output_mode）
  ├─ pipelines/route_a_deva_hybrid_video_pipeline.py（Haystack Pipeline 定義。自動版と共通）
  │    ├─ pipelines/components/video_model_components.py
  │    │     ├─ VideoReader / VideoWriter / FrameSequenceWriter / TrackingOverlayWriter
  │    │     └─ SAM2VideoPropagator（SAM2.1 動画伝搬。checkpoints/SAM2/sam2.1_hiera_large.pt）
  │    ├─ pipelines/components/deva_semi_online_tracker.py（DevaSemiOnlineTracker）
  │    │     ├─ pipelines/components/detection_island.py（GroundingDINO 検出島）
  │    │     ├─ pipelines/components/consensus.py（IoU consensus マージ、純関数）
  │    │     └─ pipelines/components/video_model_components.SAM2VideoPropagator（再掲）
  │    ├─ pipelines/components/hybrid_alpha_components.py（BEN2TransparentHybridVideoExtractor）
  │    │     ├─ pipelines/components/ben2_components.py（BEN2Extractor）
  │    │     ├─ pipelines/components/model_components.py（TransparentBGExtractor / default_device）
  │    │     ├─ pipelines/components/route_a_common.py（config/route_a.toml 読込・alpha_to_rgba）
  │    │     ├─ pipelines/components/common.py（compose_alpha 等の純関数）
  │    │     └─ pipelines/components/video_common.py（frame/mask 共通処理）
  │    └─ pipelines/components/video_common.py（build_frame_mask_sequence）
  └─ pipelines/sam2_tb_video_pipeline.py（build_video_reader_pipeline: 先頭フレームプレビュー用）
```

- 手動プロンプト版は自動版（`_deva_for_Movie.py`）と同じ
  `build_sam2_ben2_tb_deva_hybrid_pipeline`（`route_a_deva_hybrid_video_pipeline.py`）を
  そのまま再利用しており、追加で必要になるモジュールは
  `pipelines/components/ui_helpers.py`（新規、外部依存なし・cv2/gradio/numpy/common.pyのみ）と
  `GroundingDINOMultiBoxDetector`（`model_components.py` に既存）のみ。**新規の外部モデル依存はない**。
- `pipelines/components/model_registry.py` と `config/inference_models.toml` はこのアプリでは**未使用**
  （detector/tracker は GroundingDINODetector 系・SAM2VideoPropagator の既定値を直接使用しており、
  モデル切替 UI は持たない）。
- MAM 本体（`networks/`, `dataloader/`, `evaluation/`, `trainer.py`, `mam_vit*.pth`, `segment-anything/`）は
  このアプリの実行経路に**含まれない**（DEVA/BEN2/TB ハイブリッド動画専用アプリのため）。

## 2. 最小ファイル構成（案）

親会社へ渡すリポジトリは「このアプリ 1 本が動く」ことだけを目的とし、以下のみを含める。
（ユーザー指示により **checkpoints は同梱せず**、**tests も含めない**）

```
matting-anything-hybrid-video/
├── README.md                                  # セットアップ手順（本リポジトリ用に新規作成）
├── LICENSE                                     # 既存ライセンスを踏襲
├── 2026-07-01_使用モデルチェックポイントサイズとVRAM概算.md  # ルート直下に複製配置（調査\から複製）
├── Haystack_パイプライン機能説明書.md            # Haystackパイプライン全体の非エンジニア向け解説
├── QUICKSTART_uv_local.md                      # uv環境構築手順（手動版アプリに合わせ更新済み）
├── QUICKSTART_DEVA_BEN2_TB_HYBRID_MANUAL.md    # 対象アプリの使い方クイックスタート
├── .gitignore                                  # checkpoints/ outputs/ .venv/ 等を除外
├── .gitmodules                                 # samurai サブモジュール定義（sam2 fork取得用）
├── pyproject.toml                              # uv 用環境定義（torch cu124 index 含む）
├── uv.lock                                     # uv 環境ロックファイル（再現性担保）
├── requirements.txt                            # pip 運用時の代替手段（.venv + pip）
├── run_movie_apps.bat                          # 起動用バッチ（Windows）
├── gradio_app_sam2_ben2_tb_hybrid_deva_manual_for_Movie.py   # ★対象アプリ本体（手動プロンプト版）
├── config/
│   └── route_a.toml                            # alpha/blur_guide/composite/deva パラメータ
├── pipelines/
│   ├── __init__.py
│   ├── route_a_deva_hybrid_video_pipeline.py
│   ├── sam2_tb_video_pipeline.py               # build_video_reader_pipeline（先頭フレームプレビュー用）のみ使用
│   └── components/
│       ├── __init__.py
│       ├── video_model_components.py
│       ├── deva_semi_online_tracker.py
│       ├── detection_island.py
│       ├── consensus.py
│       ├── hybrid_alpha_components.py
│       ├── ben2_components.py
│       ├── model_components.py
│       ├── route_a_common.py
│       ├── common.py
│       ├── video_common.py
│       ├── ui_helpers.py                       # 手動 box/point プロンプト Canvas 用（新規追加分）
│       └── model_registry.py                   # 本アプリでは未使用だが `pipelines/components/__init__.py`
│                                                 # が re-export するため import 時に必須（後述の検証で判明）
├── GroundingDINO/                              # pip install -e 用ソース（重みは含まない, 約0.7MB）
├── samurai/                                    # git submodule（sam2 フォーク。sam2 パッケージ配布元）
│   └── sam2/                                   # `pip install -e samurai/sam2` 対象（約25MB）
└── outputs/                                    # git管理外。実行時生成（空ディレクトリ or .gitkeep のみ）
```

- **checkpoints/ は配布リポジトリに含めない**（.gitignore で除外）。README に以下を明記する:
  - `checkpoints/groundingdino_swint_ogc.pth`（約662MB）: GroundingDINO公式リリースからDL
  - `checkpoints/SAM2/sam2.1_hiera_large.pt`（約857MB）: SAM2公式からDL
  - `checkpoints/BEN2/`: 初回推論時に HuggingFace `PramaLLC/BEN2` から自動取得（ネット接続必須）
  - `checkpoints/transparent_BG/`: 初回推論時に `transparent-background` パッケージが自動DL
- **tests/ は配布リポジトリに含めない**（社内開発用のみで保持）。

### 環境作成に必須のファイル（最低限）

| ファイル | 役割 |
|---|---|
| `pyproject.toml` + `uv.lock` | uv 運用時の環境再現（本アプリの想定実行環境） |
| `requirements.txt` | pip 運用時の代替（本リポジトリ既定の `.venv + pip` 方式） |
| `.gitmodules` + `samurai/`（submodule） | `sam2`（SAM2.1 動画伝搬）パッケージの取得元 |
| `GroundingDINO/`（サブディレクトリ、重みなし） | `groundingdino` パッケージのソース。CUDA拡張ビルドに必要 |
| `README.md`（新規作成） | conda/venv作成 → `pip install -r requirements.txt` →
  `pip install -e samurai/sam2` → `pip install -e GroundingDINO`（`BUILD_WITH_CUDA=True` 必須）→
  重み配置手順、を明記 |

### 除外して良いもの（対象アプリが使わないため）

- `networks/`, `dataloader/`, `evaluation/`, `trainer.py`, `main.py`, `Matting_Anything*.ipynb`,
  `Sam2_Transparent_Background_Haystack*.py/ipynb`, `Sam2_BEN2_RouteA_for_Movie.py/ipynb`,
  `gradio_app.py`, `gradio_app_haystack.py`, `gradio_app_sam2_ben2_route_a*.py`,
  `gradio_app_sam2_ben2_tb_hybrid_deva_for_Movie.py`（自動版。今回渡すのは手動版のみ）,
  `gradio_app_sam2_transparent_BG*.py`, `inference_benchmark.py`, `run_video_matting_headless.py`
- `segment-anything/`（MAM ViT-B/L/H 用。本アプリは SAM2 のみ使用）
- `checkpoints/`（重み本体。**配布リポジトリから除外** — 決定済み）
- `config/MAM-*.toml`, `config/inference_models.toml`（本アプリ未使用。ただし将来モデル切替UIを
  追加するなら `inference_models.toml` は有用。今回スコープ外）
- `tests/`（**配布リポジトリに含めない** — 決定済み。社内開発用のみで保持）
- `assets/`, `archive/`, 日本語運用フォルダ（`エラーログ/セッションログ/報告書/計画書/調査/資料`）は
  社内運用ドキュメントのため非公開範囲（親会社へは README に必要事項のみ抜粋して渡す）

## 3. 確定・未確定事項

### 確定済み（ユーザー指示、2026-07-01 夜）

1. 配布対象アプリは `gradio_app_sam2_ben2_tb_hybrid_deva_manual_for_Movie.py`（手動プロンプト版）に変更。
2. `checkpoints/`（重み本体）は配布リポジトリから除外。README にDL手順/自動取得手順のみ記載。
3. `tests/` は配布リポジトリに含めない。
4. ライセンス表記は本ファイルではなく
   `調査\2026-07-01_使用モデルチェックポイントサイズとVRAM概算.md` に追記する（別途対応済み）。
5. 以下4ドキュメントも新リポジトリに同梱する（2026-07-01 夜、追加指示）:
   - `2026-07-01_使用モデルチェックポイントサイズとVRAM概算.md`
     （`調査\` 内の原本を**複製**してリポジトリルート直下に配置。複製済み: 両ファイルの内容は同一）
   - `QUICKSTART_DEVA_BEN2_TB_HYBRID_MANUAL.md`（対象アプリ専用クイックスタート、既存のまま同梱可）
   - `QUICKSTART_uv_local.md`（uv環境構築の汎用クイックスタート。対象アプリ変更に合わせて
     起動コマンド・スモークテスト・GroundingDINO関連の注記を更新済み。
     ※同ファイル内 §2-3 の `pytest` 実行手順は本リポジトリの `tests/`
     を前提とするため、tests未同梱の配布リポジトリでは該当しない。配布用に流用する際は
     README側でこの節を除外/読み替える旨を明記すること）
   - `Haystack_パイプライン機能説明書.md`（非エンジニア向けHaystackパイプライン解説。
     系統C＝本アプリ該当箇所のファイル参照を手動版アプリ名併記へ更新済み。系統A/B/D等
     本リポジトリに含めない他アプリの説明も残るが、参考情報として同梱してよい）

### 未確定事項（要ユーザー判断）

- （解消済み）実際の新規GitHubリポジトリは `https://github.com/kechirojp/leader-bg-removal.git`
  として作成済み。ステージング検証（後述）を経て本リポジトリへ push 実施。

## 4. 実施記録（2026-07-01 夜、push実施）

- ステージング先: セッション作業フォルダ内に上記2章の構成でファイルを複製し、
  `git init` → `git submodule add https://github.com/yangchris11/samurai.git samurai` →
  リモート `https://github.com/kechirojp/leader-bg-removal.git` へ push。
- **push前に import 検証を実施**: メインリポジトリの `.venv` から
  `sys.path` にステージングディレクトリを追加し、
  `gradio_app_sam2_ben2_tb_hybrid_deva_manual_for_Movie.py` の import と `--help` 実行を確認。
  → 当初の構成案では `pipelines/components/model_registry.py` が不足しており
  `ModuleNotFoundError` が発生した（`pipelines/components/__init__.py` が
  `model_registry` から複数シンボルを re-export しているため、本アプリが直接使わなくても
  **パッケージ import 時の必須ファイル**であることが判明）。`model_registry.py` を追加して
  import 成功・`--help` 出力を確認済み（2章のツリーに反映済み）。
- README.md は新規作成（既存リポジトリの `README.md` を流用せず、本アプリ専用に書き下ろし）。
  セットアップ（uv/pip 両方）・重み配置手順・ライセンス表記・ディレクトリ構成を記載。
- `.gitignore` も新規作成（`checkpoints/`, `outputs/`, `.venv/` 等を除外する軽量版）。
- `run_movie_apps.bat`（RouteA/TB向け旧バッチ）は同梱せず、本アプリ専用の `run_app.bat` を新規作成。

> 本ファイルは調査記録用途に加え、実施ログとしても更新。次回同種の分離作業を行う際は
> 「`pipelines/components/__init__.py` の re-export により model_registry.py が実質必須」
> という点に注意すること。

## 5. Push完了（2026-07-01 21:03 頃）

- リモート `https://github.com/kechirojp/leader-bg-removal.git`（ユーザーが事前作成、空リポジトリ）
  に `main` ブランチを push 済み。コミット: `Initial commit: leader-bg-removal minimal repo
  (SAM2+BEN2/TB DEVA-hybrid manual-prompt movie app)`。
- `samurai` は `git submodule add https://github.com/yangchris11/samurai.git samurai` で追加
  （通常の submodule。フル clone のうち本アプリが使うのは `samurai/sam2` サブディレクトリのみ）。
- README.md はユーザー確認どおり**新規作成**（元リポジトリの README は流用せず、
  本アプリ専用に書き下ろし。セットアップ2方式・重み配置手順・ライセンス・ディレクトリ構成を記載）。
- `.gitignore` / `run_app.bat` も本リポジトリ用に新規作成（元リポジトリのファイルは使い回さず）。
- 本リポジトリは「単にプロジェクトを渡すためのもの」（ユーザー方針）であり、
  今後の本開発は元の `Matting-Anything` リポジトリで継続する。

## 6. LICENSE ファイル削除（2026-07-01 21:27 頃、ユーザー指示）

- ユーザー指示: 「ライセンスファイルは入れないでください　親会社が決めることなので」
- 対応: `leader-bg-removal` リポジトリから `LICENSE`（MIT, Copyright SHI Labs）ファイルを削除し、
  README.md の「ライセンス・クレジット」節も「本リポジトリ自体は `LICENSE`（MIT License）に従います」
  という記述を削除。**サードパーティモデル（GroundingDINO/SAM2/SAMURAI/BEN2/transparent-background/
  Haystack/Gradio）のライセンス情報は事実情報として残した**（配布リポジトリ自体のライセンスは
  未設定にするが、利用モデルの第三者ライセンス表記は法的に必要な情報のため維持）。
- push 済み（コミット `5244502`: "Remove LICENSE file (licensing decision deferred to receiving company)"）。

## 7. inputs/ outputs/ フォルダ追加（2026-07-01 21:40 頃、ユーザー指示）

- ユーザー指示: 「inputsフォルダ　outputフォルダをルート直下に入れておきましょう　空のフォルダです」
- 対応: `.gitignore` を `outputs/*` + `!outputs/.gitkeep`、`inputs/*` + `!inputs/.gitkeep` に変更し、
  実行時生成物は除外したまま空フォルダ自体は git 管理下に置けるようにした。
  `inputs/.gitkeep` / `outputs/.gitkeep` を作成。
- 事前確認: アプリ本体に `inputs/` への直接参照（デフォルト入力ディレクトリ指定など）は無く、
  Gradio の動画アップロードUIから任意のファイルを選択する方式のため、`inputs/` は
  「サンプル動画を置く任意の置き場所」という位置づけ（無くても動作に支障はない）。
- README.md の「ディレクトリ構成」節に `inputs/`（任意の動画置き場）・`outputs/`（出力生成先）を追記。
- push 済み（コミット `d36b99d`: "Add empty inputs/ and outputs/ folders"）。
